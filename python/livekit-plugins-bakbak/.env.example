# Copy to `.env` and fill in values. `.env` is gitignored.
# Load before tests: `set -a && source .env && set +a`  (bash/zsh)

BAKBAK_API_KEY=
# Optional — defaults to production hub
# BAKBAK_BASE_URL=https://hub.getraya.app
